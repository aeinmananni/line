export type LinksType = {
     id:string;
     title:string;
     icon:React.ReactNode;
     link:string;
}


export type DataTypeItems = {
    title: string,
    description: string,
    price: number,
    image: string,
}